<%= form_for @recipient, url: "/users/#{session[:user_id]}/recipients" do |f| %>
  <%= f.hidden_field :user_id, value: session[:user_id] %><br>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label :email %>
  <%= f.text_field :email %><br>
  <%= f.submit %>
<% end %>
